apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: worldcup-rag

resources:
  - namespace.yaml
  - env-configmap.yaml
  - postgres.yaml
  - redis.yaml
  - api.yaml
  - worker.yaml
  - prometheus.yaml
  - grafana-configmap.yaml
  - grafana.yaml

images:
  - name: worldcup-rag
    newName: worldcup-rag
    newTag: local
